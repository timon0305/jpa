server:
  port: 8081

spring:
  datasource:
    driver-class-name: "com.mysql.jdbc.Driver"
    url: "jdbc:mysql://127.0.0.1:3306/products_dev?useSSL=false"
    username: root
    password:

  jpa:
    hibernate:
      ddl-auto: none

logging:
  level:
    root: ERROR
    com.archil: DEBUG

cors:
  allowed:
    origins: "*"

